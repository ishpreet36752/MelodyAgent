# Production Environment Variables

# Frontend (.env file in root directory)
VITE_SPOTIFY_CLIENT_ID=your_spotify_client_id_here
VITE_SPOTIFY_REDIRECT_URI=https://yourdomain.com/callback
VITE_BACKEND_URI=https://yourdomain.com/api
VITE_HF_TOKEN=your_huggingface_api_token_here

# Backend (.env file in backend directory)
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here
REDIRECT_URI=https://yourdomain.com/api/callback
FRONTEND_URI=https://yourdomain.com

# Important Notes:
# 1. Update yourdomain.com with your actual domain
# 2. Add these redirect URIs to your Spotify Developer Dashboard:
#    - https://yourdomain.com/callback (for frontend)
#    - https://yourdomain.com/api/callback (for backend)
# 3. Ensure your backend is deployed and accessible at /api endpoint
# 4. Update CORS settings in backend if needed
